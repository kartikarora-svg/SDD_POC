# Core FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
alembic==1.13.0
# Use psycopg (v3) instead - pure Python with binary packages for Windows
psycopg[binary]==3.1.13
psycopg-pool==3.2.0

# Redis
redis==5.0.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# AI/ML & RAG
langchain==0.0.340
chromadb==0.4.18
groq==0.4.1

# Document Processing
PyPDF2==3.0.1
pdf2image==1.16.3
pytesseract==0.3.10
# Note: poppler-utils not available on PyPI for Windows
# Download Poppler for Windows from: https://github.com/oschwartz10612/poppler-windows/releases/

# Export Capabilities
# WeasyPrint has complex Windows dependencies, install separately if needed
# pip install WeasyPrint (requires GTK3 runtime on Windows)
python-docx==1.1.0
reportlab==4.0.7  # Alternative PDF library that works better on Windows

# Financial Data
yfinance==0.2.32

# News Scraping
feedparser==6.0.10

# Background Jobs
APScheduler==3.10.4

# HTTP Client
httpx==0.25.1

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Development
black==23.11.0
flake8==6.1.0
mypy==1.7.1

